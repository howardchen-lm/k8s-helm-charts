# k8s-helm-charts

```bash
# if we already have the official logicmonitor repository, remove it first and run the following command.
$ helm repo add logicmonitor https://howardchen-lm.github.io/k8s-helm-charts/docs
```
